type Query {
    event(id: Int): Event
}

type Event {
    id: Int!
    title: String!
    description: String!
    organizer: String!
    location: String!
    lat: Float
    lng: Float
    startDate: String!
    endDate: String!
    photo: String!
    category: String!
}
